apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: eglobalshop
  labels:
    app: shipping-service
    app.kubernetes.io/name: shipping
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: microservice
    app.kubernetes.io/part-of: eglobalshop-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shipping-service
  template:
    metadata:
      labels:
        app: shipping-service
        app.kubernetes.io/name: shipping
        app.kubernetes.io/component: microservice
    spec:
      containers:
        - name: shipping
          image: localhost/eglobalshop-shipping-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: shipping-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 5
      restartPolicy: Always
