apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-service
  namespace: eglobalshop
  labels:
    app: billing-service
    app.kubernetes.io/name: billing
    app.kubernetes.io/version: "1.0.0"
    app.kubernetes.io/component: microservice
    app.kubernetes.io/part-of: eglobalshop-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: billing-service
  template:
    metadata:
      labels:
        app: billing-service
        app.kubernetes.io/name: billing
        app.kubernetes.io/component: microservice
    spec:
      containers:
        - name: billing
          image: localhost/eglobalshop-billing-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 9000
              name: http
          envFrom:
            - configMapRef:
                name: billing-config
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 9000
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: 9000
            initialDelaySeconds: 15
            periodSeconds: 5
      restartPolicy: Always
